# 认知科学导论大作业完成总结

## 1. 这个大作业的核心任务

本次大作业的目标，是基于给定的 EEG 数据完成一个二分类任务：判断每个样本属于 `background` 还是 `target`。

从项目说明、数据 README 和现有代码来看，整个作业最终不是只写出一个能运行的模型，而是要完成一整套可复现的实验流程，至少包括：

1. 准备并检查数据。
2. 设计并训练一个分类模型。
3. 对测试集生成预测结果文件。
4. 分析实验过程与结果。
5. 整理代码、模型、报告和视频，按要求打包提交。

## 2. 当前项目已经提供了什么

当前代码其实已经给出了一套“能跑通的基线方案”，只是效果大概率不会太强，仍需要继续改进。

### 2.1 数据层面

- `data/train/`：训练集 EEG `.npy` 文件。
- `data/test/`：测试集 EEG `.npy` 文件。
- `data/train_labels.csv`：训练标签文件，至少包含 `eeg_file` 和 `label` 两列。

根据说明文档，每个 EEG 样本的形状可以理解为 `1 x 59 x 282`，也就是单输入通道、59 个脑电通道、282 个时间点。

### 2.2 代码层面

当前各脚本作用如下：

- `load_data.py`：读取 `.npy` 数据，按样本做标准化，并把标签从文本映射为数字。
- `model.py`：定义了一个非常基础的全连接分类器，做法是先展平输入，再经过两层线性层输出二分类结果。
- `train.py`：完成训练集/验证集划分、模型训练、验证评估，并保存最佳模型和最终模型。
- `test.py`：加载训练好的最佳模型，对测试集逐个样本预测，并输出 `res/predictions.csv`。
- `utils.py`：提供随机种子固定和设备选择功能。

### 2.3 当前基线已经实现的流程

从代码可以看出，项目已经完成了以下基础流程：

1. 从 `train_labels.csv` 读取训练样本文件名和标签。
2. 按类别分层划分训练集和验证集。
3. 对每个 EEG 样本做均值方差标准化。
4. 使用 PyTorch 训练二分类模型。
5. 使用带类别权重的交叉熵损失，缓解类别不平衡问题。
6. 在验证集上选择最佳模型，并保存到 `models/best_model.pth`。
7. 用最佳模型对测试集推理，生成 `res/predictions.csv`。

也就是说，作业最基础的提交链路已经具备，但模型本身还是一个“示例起点”，还远没有到高分方案的程度。

## 3. 为了完成这个大作业，我们实际要做什么

如果按照作业要求和当前工程状态来拆解，真正需要完成的工作可以概括为以下几部分。

### 3.1 先把数据和环境跑通

首先要确保以下几点：

1. `data/train/`、`data/test/`、`data/train_labels.csv` 都已经按 README 说明放好。
2. 运行环境中已经安装项目依赖，至少包括 Python、PyTorch、NumPy、Pandas。
3. 先运行一次训练脚本和测试脚本，确认当前基线代码可以完整跑通。

对应命令就是：

```bash
python train.py
python test.py
```

这一步的目的不是追求高准确率，而是先确认：

- 数据读取正常。
- 模型训练正常。
- 模型文件能正确保存。
- 测试预测文件能正确生成。

如果这一步跑不通，后续优化没有意义。

### 3.2 理解当前基线方案的不足

目前 `model.py` 中的网络本质上是“展平后接全连接层”的浅层模型。它的优点是简单、稳定、容易训练；缺点是对 EEG 的通道结构和时间结构利用很弱。

因此，如果想把这个大作业做好，不能只停留在现在这个基线模型上，而是要围绕以下问题继续改进：

1. 是否能更好地利用时间维特征。
2. 是否能更好地利用不同脑电通道之间的空间关系。
3. 是否能减少过拟合，提高验证集泛化能力。
4. 是否能通过更合理的训练策略获得更高准确率。

### 3.3 重点改进模型设计

作业说明里明确说了，`model.py` 只是起点，不是推荐终版。所以真正重要的工作之一，就是把模型设计做成你们自己的方案。

可以考虑的方向包括：

1. 把当前全连接模型替换为 1D CNN、2D CNN、CNN + MLP 或更适合 EEG 的网络。
2. 尝试把通道维和时间维分开建模，比如先做时序卷积，再聚合通道特征。
3. 增加更合理的层数、隐藏维度、归一化层、Dropout 等。
4. 如果课程允许，也可以尝试传统机器学习方法作为对照实验，比如先提特征，再做 SVM、随机森林或逻辑回归。

这一部分不只是“把网络写复杂”，而是要能在报告里解释：

- 为什么这样设计。
- 它解决了什么问题。
- 相比基线模型有什么改进逻辑。

### 3.4 重点改进训练和验证策略

除了模型结构，训练流程本身也直接决定最后成绩。

当前 `train.py` 已经做了分层划分和类别加权，但还可以继续优化：

1. 调整批大小、学习率、训练轮数、权重衰减等超参数。
2. 尝试不同优化器或学习率调度策略。
3. 增加 early stopping，避免后期过拟合。
4. 多次更换随机种子，观察结果是否稳定。
5. 设计更可靠的验证方案，例如多次划分验证，或者交叉验证思路。

作业说明里特别鼓励的点包括：

- 训练策略优化。
- 验证集划分方法。
- 正则化与防止过拟合。
- 类别不平衡处理。

所以这部分应该是实验工作的重点，而不是只训练一次就直接提交。

### 3.5 做实验记录和结果分析

从评分标准和报告要求看，老师不只看最终准确率，也看你们的实验过程是否完整。因此需要系统地记录实验。

建议至少记录：

1. 每次实验使用的模型结构。
2. 关键超参数设置。
3. 验证集准确率和损失变化。
4. 和基线相比是否有提升。
5. 哪些方案有效，哪些方案无效。

最终报告里需要回答的问题，不只是“哪个模型最好”，还包括：

1. 数据有哪些特点。
2. 训练中遇到了什么问题。
3. 为什么会过拟合或欠拟合。
4. 错误样本可能有什么特点。
5. 最终方案为什么被选为提交版本。

## 4. 最终需要产出的内容

根据说明文档，完成作业后至少要产出下面这些东西。

### 4.1 代码与模型

需要准备：

1. 完整代码。
2. 训练好的主要模型文件，或者清楚的复现实验说明。

对应到当前工程，至少应该保证：

- 训练脚本可运行。
- 测试脚本可运行。
- `models/` 下有可用模型权重。

### 4.2 测试集预测结果

必须生成 `res/predictions.csv`。

该文件至少要包含两列：

1. `eeg_file`
2. `prediction`

其中 `prediction` 的取值应为：

- `background`
- `target`

当前 `test.py` 已经按照这个格式输出，因此只要模型训练完成，就可以直接生成提交结果文件。

### 4.3 大作业报告

报告至少应覆盖以下内容：

1. 任务理解与问题定义。
2. 数据特点分析。
3. 模型设计与改进思路。
4. 模型结构图。
5. 训练方法与验证方案。
6. 实验结果与误差分析。
7. 小组成员分工。
8. 如果参考了外部模型或论文，需要给出参考文献。

这意味着你们在做代码优化时，就要同步保留实验记录，否则最后很难把报告写完整。

### 4.4 讲解视频

还需要准备一个 5 分钟内的视频，通常建议包含：

1. 任务介绍。
2. 数据与模型思路。
3. 实验设置。
4. 结果展示。
5. 小组分工与总结。

### 4.5 最终压缩包

最终要把相关内容放在同一级目录下，再按要求命名压缩包提交。

从说明文档看，至少要整理好：

1. 代码。
2. 模型或复现说明。
3. 预测结果文件。
4. 大作业报告。
5. 讲解视频。

## 5. 按执行顺序来看，推荐的完成路线

如果把这个项目当作一个真正要交付的作业，建议按下面顺序推进：

1. 检查数据目录和标签文件格式是否正确。
2. 直接运行当前基线代码，确保训练和测试流程跑通。
3. 记录基线模型的验证结果，作为对照组。
4. 修改 `model.py`，设计更适合 EEG 的模型。
5. 调整 `train.py` 中的训练配置和验证方案。
6. 重复实验，比较不同模型和超参数表现。
7. 选出验证集表现最好的方案，保存最终模型。
8. 运行 `test.py` 生成正式的 `predictions.csv`。
9. 根据实验记录撰写报告，补充模型结构图和误差分析。
10. 录制讲解视频，整理提交材料并压缩打包。

## 6. 用一句话概括这个项目现在的状态

当前项目已经提供了一个可以直接训练和生成预测结果的 EEG 二分类基线框架；为了真正完成这次大作业，你们还需要在这个基线之上完成模型改进、训练验证优化、实验分析、报告撰写和最终提交材料整理。