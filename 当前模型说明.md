# 当前 EEG 分类模型说明

## 1. 模型要解决的问题

本项目的任务是对单个 EEG 样本做二分类，判断其属于 background 还是 target。

每个样本的输入形状为：

- 1 个输入通道
- 59 个脑电通道
- 282 个时间点

因此，模型既要利用时间维上的波形变化，也要利用不同脑电通道之间的空间关系。

## 2. 为什么不用最开始的全连接模型

最开始的基线模型是把整个 EEG 样本直接展平，然后接全连接层分类。

这种做法虽然简单，但有两个明显问题：

1. 它把时间结构和通道结构都打平了，模型很难显式利用 EEG 的局部时序特征。
2. 参数量相对大，但结构信息利用不足，容易在小数据集上过拟合。

因此，当前版本把基线模型换成了一个更适合 EEG 的轻量卷积网络。

## 3. 当前模型的整体思路

当前模型可以理解为一个简化版的 EEG 风格卷积网络，整体流程是：

1. 先在时间维上做卷积，提取局部时间模式。
2. 再跨所有脑电通道做空间卷积，学习不同通道之间的组合关系。
3. 再继续做一层更细的时序卷积，补充更高层的时间特征。
4. 通过池化逐步压缩时间维，减少噪声和参数量。
5. 最后把提取到的特征展平，用线性层输出二分类结果。

这类设计的核心优势是：

- 先保留 EEG 的结构，再做分类。
- 参数量比直接大规模全连接更可控。
- 对小规模 EEG 数据更友好。

## 4. 模型结构拆解

### 4.1 第一阶段：时间卷积

第一层使用卷积核大小为 1 × 64 的二维卷积。

它的作用不是在空间上混合通道，而是沿着时间轴提取短时模式，例如某些局部波形变化、峰值结构或者短窗口内的节律特征。

因为卷积核的通道方向大小是 1，所以这一层主要做的是“按时间看波形”。

### 4.2 第二阶段：空间卷积

接下来使用跨 59 个脑电通道的卷积，也就是在空间维上一次性覆盖全部通道。

这一层的作用是学习不同脑电通道之间的联合关系，例如：

- 哪些通道的响应模式会一起出现。
- 哪些通道组合更能区分 background 和 target。

这里使用了分组卷积，目的是减少参数量，同时让每组时间特征先分别学习对应的空间投影。

### 4.3 第三阶段：深度可分离时序卷积

在完成一次时间特征提取和空间特征融合后，模型又做了一层更轻量的时序卷积。

这一层包括：

1. 一层按通道分组的时间卷积。
2. 一层 1 × 1 卷积做特征混合。

这个设计的作用是：

- 继续提取更高层的时序模式。
- 控制参数量和计算量。
- 比普通大卷积更适合当前样本规模。

### 4.4 池化与分类层

在两个卷积阶段之间和之后，模型都使用了平均池化来压缩时间维。

平均池化的作用主要有三点：

1. 降低特征长度，减少后续参数量。
2. 提高局部噪声鲁棒性。
3. 让模型更关注整体趋势，而不是过度依赖单个时间点。

最终，卷积特征会被展平，然后接一个线性分类层输出两类 logits。

## 5. 训练阶段做了哪些优化

当前版本并不只是改了模型结构，训练流程本身也做了几项重要优化。

### 5.1 样本标准化

每个 EEG 样本在读入后，都会先做单样本标准化：

1. 计算该样本的均值。
2. 计算该样本的标准差。
3. 用标准化后的结果送入模型。

这样做的目的是降低不同样本整体幅值差异带来的干扰。

### 5.2 训练集数据增强

数据增强只对训练集启用，不对验证集和测试集启用。

当前用了几种比较温和的增强：

1. 时间轴平移。
2. 局部时间片段遮挡。
3. 少量通道遮挡。
4. 小幅高斯噪声扰动。

这些增强的目标不是“造出完全不同的新样本”，而是让模型对小的时间偏移、局部缺失和微弱噪声更稳。

### 5.3 损失函数和类别不平衡处理

训练集类别分布不是完全均衡的，因此训练时用了带类别权重的交叉熵损失。

这样可以避免模型过度偏向样本更多的那一类。

此外，还加入了轻微的 label smoothing，用来减轻过拟合和过度自信预测。

### 5.4 优化器与学习率调度

当前使用的是 AdamW，而不是最基础的 Adam。

这样做的主要原因是：

- 对小规模深度学习任务通常更稳定。
- 带权重衰减，正则化效果更自然。

训练过程中还使用了基于验证损失的学习率衰减策略：

- 当验证损失长期不下降时，自动把学习率减半。

这样可以让模型在训练后期更稳定地收敛。

### 5.5 梯度裁剪与早停

为了进一步稳定训练，还做了两件事：

1. 梯度裁剪，避免个别 batch 导致梯度过大。
2. 早停，如果验证集长期没有提升，就提前结束训练。

这两项措施可以减少无效训练，并降低后期过拟合风险。

## 6. 当前验证准确率是怎么来的

当前的 0.7367 不是测试集准确率，而是验证集准确率。

这是一个非常重要的区别。

项目里的测试集没有公开标签，所以无法直接计算测试集准确率。当前代码的做法是：

1. 读取完整的训练标签文件。
2. 按类别分层，从训练集中切出一部分样本作为验证集。
3. 用剩下的训练样本训练模型。
4. 在这部分“留出来但不参与训练”的验证集上计算准确率。

也就是说，当前看到的 0.7367，是模型在训练集内部划分出来的验证集上的表现。

## 7. 为什么 test 没标签还能评估模型

因为真正参与评估准确率的并不是 test 集，而是从 train 集里切出来的 validation 集。

三者关系如下：

### 7.1 训练集

- 有标签。
- 用来更新模型参数。

### 7.2 验证集

- 也来自原始训练集。
- 有标签。
- 训练时不参与参数更新。
- 只用于观察模型泛化能力和选择最佳模型。

### 7.3 测试集

- 没有公开标签。
- 只能拿来生成提交预测结果。
- 无法在本地直接算准确率。

所以，本地能算出来的准确率，本质上都是验证集准确率，不是测试集准确率。

## 8. 当前模型的优点

当前这版模型相对于最开始的全连接基线，有以下优点：

1. 能同时利用时间维和通道维结构。
2. 参数量更可控，泛化能力更好。
3. 有针对 EEG 数据的小幅增强策略。
4. 训练流程更完整，包括类别权重、学习率调度、梯度裁剪和早停。
5. 实测验证准确率明显高于最初基线。

## 9. 当前模型的局限

虽然当前版本已经比基线明显更强，但它仍然有局限：

1. 目前主要还是时域建模，没有显式引入频域或时频特征。
2. 验证方式仍然是单次划分，结果会受到划分随机性的影响。
3. 数据量不大，模型上限容易受限。
4. 还没有引入更系统的交叉验证或更强的特征工程。

## 10. 一句话概括当前方案

当前模型本质上是一个面向 EEG 二分类任务的轻量卷积网络：先提取时间模式，再融合通道关系，再配合标准化、数据增强、类别加权、学习率调度和早停来提升泛化性能，从而在当前固定验证划分上得到约 0.7367 的验证准确率。